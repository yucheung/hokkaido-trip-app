# 北海道旅遊 App — 產品規格文件 (PRD)

版本:1.0
更新日期:2026/08/22
適用行程:大興旅行社「北極星列車之旅」5日遊(2026/08/25-08/29,函館進出)
主要平台:iPhone / Expo Go
使用情境:離線速查,一家四口(47/46/18/14歲)共用參考

---

## 1. 專案目標

讓使用者在旅途中只靠這支 App,不需網路、不需另外查資料,就能掌握:
- 每日行程、餐食是否已含、住宿地點
- 出國須知中容易忽略但後果嚴重的規定(海關、行李、退稅、登機)
- (次要)景點的美食/看點/小提醒
- (次要)依地點分類的藥妝/3C購物建議與官方優惠券

**非目標(明確排除)**:
- 不做多人雲端同步(純本機儲存,AsyncStorage)
- 不做飯店電話撥號功能(僅文字顯示)
- 不做花費記帳/分帳
- 不做帳號登入系統

---

## 2. 技術基礎設定

- 框架:Expo Go(React Native)
- 目標平台:iOS(iPhone),不需考慮 Android 特殊適配
- 資料儲存:純本機。第一版資料直接內建(打包進 app 的靜態 JSON,見 `data.json`),不需後端、不需 API
- 離線優先:App 啟動與所有頁面渲染,不得依賴網路請求。所有圖片如有使用,建議用 icon 字型或向量圖形(如 `@expo/vector-icons`),避免依賴遠端圖片載入失敗造成空白
- 狀態管理:輕量即可(React useState/useContext),不需要 Redux 等重量方案

---

## 3. 資訊架構與優先順序

App 採**底部 Tab Bar** 導覽,共 4 個分頁。開發依下列優先順序分兩階段執行:

### P0(必做,第一階段)
| Tab | 名稱 | 說明 |
|---|---|---|
| 1 | 今日行程 | 首頁,5天行程卡片列表 + 每日詳情頁 |
| 2 | 旅遊須知 | 海關/行李/退稅/登機/打包眉角 + 航班資訊 + 緊急聯絡 |

### P1(次要,第二階段,待 P0 完成後再開工)
| Tab | 名稱 | 說明 |
|---|---|---|
| 3 | 景點百科 | 11+ 個景點的美食/看點/提醒卡片 |
| 4 | 購物指南 | 依地點分類的藥妝/3C 建議 + 官方優惠券 |

> **重要**:P0 兩個分頁必須先完整可用、可獨立驗收,才進入 P1 開發。若時間不足,P1 可以延後或省略,不影響 P0 的完整性。Tab Bar 在 P0 階段可以先只顯示 2 個分頁,P1 完成後再加回另外 2 個。

---

## 4. 資料模型(對應 `data.json`)

所有畫面資料一律從 `data.json` 讀取,不在元件內寫死內容(icon、顏色、排版邏輯除外)。

```typescript
interface Trip {
  day: number;                // 1-5
  date: string;               // "2026-08-25"
  weekday: string;            // "二"
  title: string;              // 當日重點一句話,如「函館山夜景」
  fullItinerary: string;      // 完整行程文字(含 → 符號分段)
  meals: {
    breakfast: { label: string; included: boolean };
    lunch:     { label: string; included: boolean };
    dinner:    { label: string; included: boolean };
  };
  hotelOptions: string[];     // 候選飯店名稱陣列(如「函館平成館 或 海與燈 或 …」)
  hotelPhone: string;         // 純顯示用,不做撥號功能
  relatedSpotIds: string[];   // 對應 Spot.id,供詳情頁做「更多資訊」連結(P1 功能，P0 階段此欄位可留空陣列不使用)
}

interface Spot {
  id: string;
  name: string;
  day: number[];               // 出現在哪幾天(可能對應多天)
  tagline: string;             // 特色一句話
  mustEat: string[];           // 推薦美食,條列短句,非段落
  mustSee: string[];           // 拍照/看點建議,條列短句
  tips: string[];              // 小提醒,條列短句
}

interface ShoppingGuideItem {
  location: string;            // 函館 / 登別 / 富良野美瑛 / 札幌 / 小樽
  category: "藥妝" | "3C" | "伴手禮";
  storeName: string;
  items: string[];             // 具體品項
  note: string;                // 補充說明(地址/營業時間/交通等)
}

interface OfficialCoupon {
  storeName: string;
  discount: string;            // 如「免稅10% + 出示優惠券再折7%」
  applicableLocations: string[];
  sourceNote: string;          // 固定文案:「店家官方公開發放之常態折扣,非個人推薦連結」
}

interface TravelNoticeItem {
  category: "行李規定" | "海關規定" | "退稅流程" | "登機須知" | "打包眉角";
  title: string;
  detail: string[];            // 條列短句,非段落
  severity: "info" | "warning"; // warning = 若違反會有實質後果(補稅/延誤登機等),UI 需視覺加強
}

interface FlightInfo {
  date: string;
  flightNo: string;
  airline: string;
  route: string;               // "台北桃園 → 函館" 或反向
  departTime: string;
  arriveTime: string;
  gateInfo: string;            // 「第一航站星宇航空第六櫃台」等
}

interface EmergencyContact {
  name: string;                // "緊急聯絡電話(上班時間)" 等
  phone: string;
  note?: string;
}
```

---

## 5. 畫面規格

### 5.1 今日行程(首頁)— P0

**佈局**:垂直卡片列表(非時間軸,理由:核心需求是查詢速度,卡片列表比時間軸更直覺高效)

每張卡片顯示:
- 日期 + 星期(如「08/25 (二)」)
- 當日重點一句話(Trip.title)
- 餐食狀態列:三個 icon(🍳🍱🍽️)分別代表早/午/晚餐,每個 icon 旁邊搭配顏色標籤:
  - `included: true` → 綠色勾勾 badge,顯示餐點內容簡稱
  - `included: false` → 灰色或紅色叉叉 badge,顯示「需自理」
- 當晚住宿地點縮寫(如「函館平成館 或 同等級」)

點擊卡片 → 進入「每日詳情頁」

**驗收標準**:
- [ ] 5 張卡片皆正確顯示對應日期資料
- [ ] 餐食 badge 顏色與圖示邏輯一致,included=true/false 視覺明確可辨識(不需要點進去才知道)
- [ ] 卡片可點擊,轉場順暢
- [ ] 全程離線可瀏覽,不依賴任何網路請求

---

### 5.2 每日詳情頁 — P0

**佈局**:單頁,由上至下分區塊呈現(非長篇大論的段落文字)

區塊順序:
1. 標題區:日期/星期/當日重點
2. 完整行程:將 `fullItinerary` 依「→」符號拆解為條列步驟(每個景點名稱一行,不要整段塞在一起)
3. 餐食卡片:三個獨立小卡(早/午/晚),各自顯示 included 狀態與內容
4. 住宿卡片:候選飯店列表 + 電話(純文字顯示,不可點擊撥號)

若該天有 `relatedSpotIds`(P1 完成後啟用):每個行程步驟後方加上「更多資訊 ›」按鈕,點擊彈出景點懶人包 modal

**驗收標準**:
- [ ] 行程步驟以條列呈現,非整段文字
- [ ] 餐食三小卡的 included 狀態與首頁卡片邏輯一致
- [ ] 飯店電話為純文字,確認沒有做成可點擊撥號元件
- [ ] P0 階段若 relatedSpotIds 為空陣列,「更多資訊」按鈕不顯示(不要顯示空按鈕)

---

### 5.3 旅遊須知 — P0

**佈局**:依 `TravelNoticeItem.category` 分成 5 個可折疊區塊(accordion 或 section list 皆可,但預設需全部收合,避免一次性資訊過載),區塊順序:

1. **航班資訊**(固定置頂,不折疊):去程/回程航班卡片,顯示日期/航班號/起訖時間/航廈櫃台
2. **緊急聯絡電話**(固定置頂,不折疊):列表呈現,含公司緊急電話、機場專線
3. 行李規定(可折疊)
4. 海關規定(可折疊)
5. 退稅流程(可折疊)
6. 登機須知(可折疊)
7. 打包眉角(可折疊)

每個 `TravelNoticeItem` 呈現為一張小卡:
- `severity: "warning"` 的項目,卡片邊框或標題文字使用警示色(如橘紅色),並在標題前加上 ⚠️
- `severity: "info"` 的項目使用一般色系
- `detail` 陣列以條列呈現,不寫成段落

**驗收標準**:
- [ ] 航班資訊與緊急聯絡電話固定在頁面最上方,不會被折疊隱藏
- [ ] warning 等級項目視覺上明顯區別於 info 等級(顏色或圖示至少一項不同)
- [ ] 所有 detail 內容以條列短句呈現,無長段落文字牆
- [ ] 退稅流程區塊需包含明確提示:「你們的行程適用現行制度,結帳時當場折抵,2026/11/1 起的新制與此行程無關」

---

### 5.4 景點百科 — P1

**佈局**:可搜尋/篩選的卡片列表,每張卡片呈現一個 `Spot`

每張卡片內部分三個小區塊(用不同 icon 區分,不要混在一起寫成一段):
- 🍡 推薦美食(mustEat,條列)
- 📸 拍照/看點建議(mustSee,條列)
- 💡 小提醒(tips,條列)

**篩選功能**:提供「依天數篩選」(Day 1-5)與關鍵字搜尋(比對 Spot.name)

**與每日詳情頁的連動**:詳情頁行程步驟中的地名若對應到 `Spot.id`,顯示「更多資訊 ›」,點擊後以 modal 彈出對應 Spot 卡片內容(不跳轉離開詳情頁)

**驗收標準**:
- [ ] 所有 Spot 資料正確渲染,三個分類區塊清楚分開(視覺上不會誤認為同一段文字)
- [ ] 篩選/搜尋功能可正確過濾結果
- [ ] 從每日詳情頁點擊「更多資訊」可正確彈出對應景點卡片

---

### 5.5 購物指南 — P1

**佈局**:依地點(函館/登別/富良野美瑛/札幌/小樽)分組的卡片列表,每組內再依 category(藥妝/3C/伴手禮)分小節

每張 `ShoppingGuideItem` 卡片顯示:店名、品項條列、補充說明

**官方優惠券區塊**:獨立於地點分組之上或之下,固定呈現 `OfficialCoupon` 資料,每張卡片必須顯示 `sourceNote` 文字(店家官方公開發放之常態折扣,非個人推薦連結),不可省略此標註

**驗收標準**:
- [ ] 依地點分組正確,不會把不同地點的品項混在一起
- [ ] 每張優惠券卡片皆顯示 sourceNote 免責文字
- [ ] 內容為條列式短句,非段落

---

## 6. 視覺與互動規則(全 App 通用)

- **不寫大段落文字**:任何超過 2 句話的內容,一律拆成條列(bullet list)
- **Badge 用色邏輯**(需全 App 一致):
  - 綠色/勾勾 = 已包含/沒問題/可以做
  - 灰色或紅色/叉叉 = 未包含/需自理/需注意
  - 橘紅色 = 警示等級(違反會有實質後果,如補稅、延誤登機)
- **Icon 使用**:優先使用 `@expo/vector-icons` 內建圖示庫,不使用需要網路載入的圖片,確保離線可靠
- **字級與留白**:卡片式呈現需保留足夠留白,避免資訊擁擠;標題與內文字級需有明顯層級差異
- **不做的視覺元素**:不需要複雜動畫、不需要深色模式(除非額外要求)、不需要多語系(僅繁體中文)

---

## 7. 開發完成定義(Definition of Done)

P0 完成的標準:
1. App 可在 Expo Go 中於 iPhone 上正常開啟,無 crash
2. 首頁 5 張卡片、每日詳情頁、旅遊須知頁三者資料正確對應 `data.json`,無寫死的假資料殘留
3. 全程離線可用(可關閉手機網路測試)
4. 通過第 5 章節列出的所有驗收標準 checkbox

P1 完成的標準:
1. 景點百科與購物指南可正常瀏覽、篩選
2. 每日詳情頁與景點百科之間的連動(更多資訊按鈕)運作正常
3. 通過對應章節驗收標準

---

## 8. 給執行 Agent 的注意事項

- 請先讀完本文件與 `data.json` 全文,再開始寫程式,不要邊做邊猜資料結構
- 若本文件與 `data.json` 有欄位對不上的情況,以本文件第 4 章節的 TypeScript 介面定義為準,回報落差而非自行決定
- P0 與 P1 是明確的階段切分,不要提前開始 P1 的畫面而讓 P0 延遲完成
- 若時間或技術限制無法完成 P1,請明確告知使用者「目前只完成 P0」,不要用簡化版的 P1(如只有純文字沒有 icon 分類)濫竽充數
