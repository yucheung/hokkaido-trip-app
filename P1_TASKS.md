# P1 開發任務說明

## 前提確認
- P0(今日行程 + 旅遊須知)已完成實機測試,確認可用,不需再修改
- package.json 版本衝突已修正(如尚未修正,請先完成該項再開始P1)
- 資料層已就緒:`src/data/index.ts` 已經 export 好 `spots`、`shoppingGuide`、`officialCoupons`,型別定義也已存在,**不需要重新設計資料結構**,直接使用

## 本次任務範圍

請依照 `PRD.md` 第5.4節(景點百科)與第5.5節規格,實作以下兩個新分頁,並更新Tab Bar：

### 1. 景點百科分頁
- 新增至 `src/app/(tabs)/` 底下(建議檔名 `spots.tsx`)
- 卡片列表呈現全部 `spots` 資料,每張卡片需清楚分三區塊(不可混在一段文字):
  - 🍡 推薦美食(mustEat)
  - 📸 拍照/看點建議(mustSee)
  - 💡 小提醒(tips)
  - 若該分類陣列為空,不顯示該區塊標題(避免顯示空欄位)
- 提供依天數篩選(Day 1-5)與關鍵字搜尋(比對 `name` 欄位)
- Tab Bar 新增此分頁,icon 建議使用 `Ionicons` 的 `location` 或 `book`,標題「景點百科」

### 2. 購物指南分頁
- 新增至 `src/app/(tabs)/` 底下(建議檔名 `shopping.tsx`)
- 依 `location` 分組呈現 `shoppingGuide`(函館/登別/富良野美瑛/札幌/小樽),同地點內再依 `category` 分小節(藥妝/3C/伴手禮)
- 官方優惠券(`officialCoupons`)獨立區塊呈現,**每張卡片必須顯示 `sourceNote` 文字**(不可省略),建議用較小字級但仍清楚可讀的方式呈現在卡片底部
- Tab Bar 新增此分頁,icon 建議使用 `Ionicons` 的 `bag` 或 `pricetag`,標題「購物指南」

### 3. 每日詳情頁連動(P0既有頁面的小更新)
- 在 `src/app/day/[day].tsx` 的行程步驟列表中,若該步驟文字對應到某個 `Spot.id`(透過 `trip.relatedSpotIds` 比對),該行程步驟後方加上「更多資訊 ›」按鈕
- 點擊後以 Modal 彈出對應景點的完整卡片內容(美食/看點/提醒三區塊),不要跳轉離開詳情頁
- 若 `relatedSpotIds` 為空或無對應,不顯示按鈕(維持現狀)

## 視覺規則(沿用PRD第6章,不需重新制定)
- 不寫大段落文字,一律條列呈現
- Icon 優先用 `@expo/vector-icons`,不依賴遠端圖片
- 卡片式呈現,保留足夠留白

## 驗收標準
- [ ] 景點百科所有14筆 spots 資料正確渲染,三分類區塊視覺上清楚分開
- [ ] 篩選(依天數)與搜尋功能可正確運作
- [ ] 購物指南依地點正確分組,不同地點資料不會混在一起
- [ ] 每張優惠券卡片皆顯示 sourceNote 免責文字,無一遺漏
- [ ] 每日詳情頁的「更多資訊」按鈕可正確彈出對應景點內容
- [ ] `npx tsc --noEmit` 與 `npx expo lint` 皆為零錯誤零警告
- [ ] `npm install`(不加任何額外參數)可正常成功
- [ ] 全程離線可用(可用飛航模式測試)

## 完成後請回報
- 是否所有驗收項目皆通過
- 若有任何 PRD 規格與實作有落差之處,請明確說明差異點與原因,不要逕自簡化或省略後不告知
