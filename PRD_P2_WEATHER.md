# PRD 附錄:天氣預測與穿搭建議功能

版本:1.0
更新日期:2026/08/22
本文件為 `PRD_北海道旅遊App.md` 的擴充,新增範圍屬於 **P2(第三階段)**,建立在P0+P1已完成的基礎上。

---

## 1. 功能目標

在首頁每張日期卡片上,額外顯示當天的天氣預報摘要與對應的穿搭建議,讓使用者不用切換App就能決定當天怎麼穿。

**重要架構變更提醒**:P0/P1 的設計原則是「全程離線可用,零網路依賴」。天氣預報功能**必然需要網路請求**,這是本功能唯一打破原有離線假設的部分。以下設計皆圍繞「如何優雅處理離線情境」展開,務必實作快取與錯誤處理,不能讓這個功能破壞其餘畫面的離線可靠性。

---

## 2. 資料來源

**API**:Open-Meteo(`https://api.open-meteo.com/v1/forecast`)

- 免費,非商業用途每日約1萬次請求限制,足夠使用
- **不需要 API Key**,不需註冊,直接 HTTP GET 呼叫
- 整合日本氣象廳(JMA)等官方氣象模型資料,非自行推算
- 回傳格式為 JSON,無需額外 SDK

**每日呼叫的座標**(對應 `data.json` 新增的 `weatherLocations` 欄位,已依每天白天主要活動地點設定,而非住宿地,原因是穿搭建議應貼近實際戶外活動情境):

| Day | 地點 | 緯度 | 經度 |
|---|---|---|---|
| 1 | 函館 | 41.7687 | 140.7288 |
| 2 | 登別 | 42.4141 | 141.1994 |
| 3 | 富良野 | 43.3417 | 142.3833 |
| 4 | 小樽 | 43.1907 | 140.9947 |
| 5 | 函館 | 41.7687 | 140.7288 |

**API請求範例**(一次呼叫可取得單一地點的每日預報):

```
https://api.open-meteo.com/v1/forecast
  ?latitude={lat}&longitude={lon}
  &daily=weather_code,temperature_2m_max,temperature_2m_min,precipitation_probability_max,wind_speed_10m_max,uv_index_max
  &timezone=Asia/Tokyo
  &forecast_days=7
```

**建議做法**:5天行程,5個不同地點,應對每個地點各發送一次請求(或用 Open-Meteo 支援的逗號分隔多座標一次查詢,降低請求數),而非合併成一個地點,否則預報會失準(例如富良野跟小樽的溫度天氣可能有明顯差異)。

---

## 3. 資料模型

```typescript
interface WeatherLocation {
  day: number;
  location: string;
  latitude: number;
  longitude: number;
}

interface DailyForecast {
  day: number;
  fetchedAt: string;              // ISO timestamp,記錄這筆資料是何時抓取的
  high: number;                   // 攝氏
  low: number;
  precipitationProbability: number; // 0-100
  windSpeedMax: number;           // km/h
  uvIndexMax: number;
  weatherCode: number;            // WMO weather code,原始數值
  weatherDescription: string;     // 由 weatherCode 轉換的中文描述,如「晴時多雲」
}

// 本機快取結構(儲存於 AsyncStorage,key 建議用 "weatherCache")
interface WeatherCache {
  [day: number]: DailyForecast;
}
```

`WeatherLocation` 資料已寫入 `data.json` 的 `weatherLocations` 欄位,可直接讀取使用,不需要重新輸入座標。

---

## 4. WMO Weather Code 對照表(用於 weatherDescription 轉換)

Open-Meteo 回傳的 `weather_code` 是 WMO 標準代碼,需要轉換成中文描述與 icon。常用對照(依本行程氣候特性精簡):

| Code | 中文描述 | 建議 icon(Ionicons) |
|---|---|---|
| 0 | 晴朗 | sunny |
| 1-2 | 晴時多雲 | partly-sunny |
| 3 | 多雲 | cloudy |
| 45, 48 | 有霧 | cloud |
| 51-57 | 毛毛雨 | rainy |
| 61-67 | 有雨 | rainy |
| 71-77 | 有雪 | snow |
| 80-82 | 陣雨 | rainy |
| 95-99 | 雷雨 | thunderstorm |

若遇到未列出的 code,顯示「天氣多變」與預設 icon,不可讓 App 因未知 code 而出錯或空白。

---

## 5. 穿搭建議邏輯(規則式運算,非固定文字)

因為溫度、降雨、紫外線每天數值不同,穿搭建議**不能寫死內容**,必須用條件邏輯即時運算產生。以下是規則規格,請依此實作為一個純函式(輸入 `DailyForecast`,輸出建議字串陣列):

### 依最高溫(high)判斷基本穿著

| 溫度範圍 | 建議 |
|---|---|
| ≥ 30°C | 極輕薄短袖為主,務必加強防曬與補水 |
| 25-29°C | 短袖即可,早晚溫差大建議備一件薄外套 |
| 20-24°C | 短袖或薄長袖皆可,建議攜帶薄外套應對早晚與室內冷氣 |
| 15-19°C | 建議長袖搭配薄外套 |
| < 15°C | 建議長袖+保暖外套,必要時加內搭 |

### 依降雨機率(precipitationProbability)判斷

| 機率範圍 | 建議 |
|---|---|
| ≥ 60% | 建議攜帶雨具,並考慮防水鞋款 |
| 30-59% | 建議隨身攜帶輕便雨具備用 |
| < 30% | 不特別需要準備雨具 |

### 依紫外線指數(uvIndexMax)判斷

| 指數範圍 | 建議 |
|---|---|
| ≥ 8(極強) | 務必使用防曬乳、帽子、太陽眼鏡,盡量避免正午長時間曝曬 |
| 6-7(強) | 建議使用防曬乳與帽子 |
| < 6 | 一般防曬即可 |

### 依風速(windSpeedMax)判斷(額外提醒,非必要顯示)

| 風速範圍(km/h) | 建議 |
|---|---|
| ≥ 40 | 若當天行程含高處景點(如函館山纜車),請特別注意保暖與固定隨身物品 |
| < 40 | 不特別提示 |

**輸出格式**:將命中的規則建議合併為一個字串陣列(每項規則最多輸出一句),用於卡片上的條列顯示,不要合併成一段長文字。

---

## 5.5 GPS輔助定位(次要功能,非預設行為)

**定位方式**:使用 `expo-location`(Expo Go 原生支援,前景定位即可,不需要 development build)

**設計原則**:GPS定位是**輔助/次要**功能,不取代預設的寫死座標。原因是行程為跟團行程,每天地點已排定,寫死座標更可靠、不受GPS訊號或權限影響;GPS僅用於使用者臨時想查詢「我現在所在位置」的天氣時使用。

**互動流程**:
1. 卡片天氣區塊旁提供一個「📍使用目前位置」按鈕(次要視覺權重,不與主要的手動刷新按鈕搶視覺焦點)
2. 點擊後呼叫 `Location.requestForegroundPermissionsAsync()` 請求前景定位權限
3. 若使用者拒絕權限:顯示提示文字「無法取得目前位置,已顯示行程預設地點天氣」,不阻斷其餘功能
4. 若取得權限成功:呼叫 `Location.getCurrentPositionAsync()` 取得座標,以此座標重新請求 Open-Meteo API,暫時覆蓋該卡片顯示的天氣資料
5. 覆蓋顯示時,需清楚標示「目前位置」字樣與圖示,讓使用者分辨這是GPS結果還是預設行程地點結果,避免混淆
6. 此為單次查詢(one-shot),不需要背景持續追蹤位置,也不需要訂閱位置更新(`watchPositionAsync`),避免不必要的權限與電量消耗

**錯誤處理**:
- GPS訊號取得逾時(建議 10 秒),顯示「定位逾時,顯示預設地點天氣」並自動退回原本資料
- 室內或訊號不良導致定位失敗,同樣優雅退回預設資料,不crash

---

## 6. 快取與更新機制

- **自動抓取**:App啟動時,若快取中沒有當天日期範圍內的資料,或快取的 `fetchedAt` 已超過某個合理時效(建議 6 小時),自動發送請求更新
- **手動重新整理按鈕**:每張日期卡片或首頁頂部,提供一個手動刷新的按鈕/圖示(建議用 `Ionicons` 的 `refresh` icon),點擊後強制重新請求該日或全部日期的預報,不管快取是否過期
- **離線情境處理**:
  - 若有快取資料但目前無網路,顯示快取資料,並在旁邊標示「更新於 X月X日 XX:XX」的時間戳,讓使用者知道這不是即時資料
  - 若完全沒有快取資料且無網路,顯示「尚無天氣資料,請連網後重新整理」的提示文字,**不可讓 App crash 或顯示空白區域**
  - 網路請求失敗時(非離線,而是 API 暫時無回應),同樣優雅降級為顯示快取或提示文字,並在 console 記錄錯誤,不對使用者顯示技術性錯誤訊息

---

## 7. UI 規格(整合進首頁卡片)

在既有首頁卡片(P0規格 5.1節)的餐食狀態列之後,新增一個天氣區塊。視覺層次由上至下:

1. **日期/星期/Day數 + 地點標籤**(沿用既有卡片頭部,右上角可加小字地點名稱)
2. **當日重點標題**(沿用既有設計)
3. **餐食badge列**(沿用既有設計,不更動)
4. **分隔線後,天氣區塊**:
   - 左側:天氣icon(依weatherCode對照,建議24-28px大小)+ 高溫/低溫(較大字級,視覺主角)+ 降雨機率(較小字級,同一行右側)
   - 右側:兩個小icon按鈕並排——「使用目前位置」(GPS icon)與「重新整理」(refresh icon),兩者視覺權重需小於溫度數字,避免搶走主要資訊的焦點
5. **穿搭建議卡片**:用有背景色的小卡片呈現(區別於一般文字),內含一個穿搭相關icon + 建議文字(取命中規則的第一條最主要建議,避免卡片過長);點擊可展開顯示完整建議清單(溫度+降雨+紫外線+風速全部命中的規則)
6. **更新時間戳**:最小字級、最淡顏色,呈現「更新於 X月X日 XX:XX」,不干擾主要資訊視覺焦點
7. **住宿資訊**(沿用既有設計,置於天氣區塊下方,用分隔線區隔)

**設計原則**:天氣區塊的視覺重點應該是「溫度數字」與「穿搭建議」這兩項使用者最想快速掃到的資訊,GPS/刷新按鈕與更新時間戳屬於輔助功能,視覺上刻意做得比較不顯眼(較小尺寸、較淡顏色),避免資訊擁擠感,呼應PRD主文件第6章「不寫大段落文字」與「保留足夠留白」的原則。

**顏色語意建議**(呼應已建立的badge顏色邏輯):
- 天氣icon與溫度文字:使用中性強調色(非成功/失敗語意,單純資訊呈現)
- 穿搭建議卡片:使用淡色底(如藍色系淡底)+ 對應深色文字,與餐食badge的綠/紅語意色區隔開來,避免使用者誤解穿搭建議也是某種「狀態判定」

---

## 8. 錯誤處理與邊界情況檢查清單

- [ ] API 完全無回應(逾時)時,不可讓整個首頁卡在載入畫面,需有明確的逾時機制(建議 8-10 秒)並降級顯示快取或提示
- [ ] 5個地點若部分成功部分失敗(例如函館抓到了,登別失敗),個別卡片獨立顯示各自狀態,不應因為其中一個地點失敗就讓全部卡片都顯示錯誤
- [ ] `weather_code` 遇到未列在對照表中的數值時,需有預設值,不可讓程式出錯
- [ ] 手動刷新按鈕在請求進行中應有 loading 狀態提示(如轉圈圈 icon),避免使用者重複點擊觸發多次請求

---

## 9. 驗收標準

- [ ] 5張首頁卡片皆正確顯示對應地點的天氣摘要(高溫/低溫/降雨機率)與至少一條穿搭建議
- [ ] 開啟飛航模式後,若先前已成功抓取過資料,卡片仍顯示快取內容並正確標示更新時間
- [ ] 開啟飛航模式且從未抓取過資料時,顯示「尚無天氣資料」提示,不crash、不空白
- [ ] 手動刷新按鈕可正確觸發重新請求,並更新 `fetchedAt` 時間戳
- [ ] 穿搭建議內容會隨天氣數值不同而變化(可用不同城市對照驗證,例如函館與富良野若溫度不同,建議文字應不同)
- [ ] `npx tsc --noEmit` 與 `npx expo lint` 皆為零錯誤零警告
- [ ] 此功能的網路失敗不會影響其餘既有分頁(旅遊須知/景點百科/購物指南)的離線可用性

---

## 10. 給執行 Agent 的注意事項

- 穿搭建議邏輯**必須用函式運算**,不要為了省事把常見溫度區間的建議文字寫死存進 `data.json`——那樣無法對應每天實際天氣數值的變化
- `weatherLocations` 座標資料已存在 `data.json`,直接讀取使用,不需要重新查詢或猜測座標
- 若技術上發現 Open-Meteo 的免費額度或回應格式與本文件描述有落差,請如實回報落差,不要自行決定簡化或省略錯誤處理邏輯
